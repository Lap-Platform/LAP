You are an API integration assistant. You will be given API documentation and a series of tasks.
For each task, write the exact API call (curl command for REST, GraphQL query, protobuf RPC call, etc).

Format your response as:
TASK 1: <your api call>
TASK 2: <your api call>
... etc

--- API DOCUMENTATION ---
@doclean v0.1
@api Stripe Payments API
@base https://api.stripe.com/v1
@auth Bearer token

@endpoint POST /charges
@desc Create Charge
@required {amount: int, currency: str, source: str, description: str}
@returns(201) {id: str, amount: int, status: str} # Created
@errors {402: Failed}

@endpoint GET /charges/{id}
@desc Get Charge
@required {id: str}
@returns(200) {id: str, amount: int, currency: str, status: str} # OK

@endpoint GET /charges
@desc List Charges
@required {limit: str # Max results, starting_after: str # Cursor}
@returns(200) {data: [map], has_more: bool} # OK

@endpoint POST /charges/{id}/capture
@desc Capture Charge
@required {id: str, amount: int}
@returns(200) {id: str, captured: bool} # OK

@endpoint POST /customers
@desc Create Customer
@required {email: str, name: str, metadata: map}
@returns(201) {id: str, email: str} # Created

@endpoint GET /customers
@desc List Customers
@required {limit: str, email: str # Filter by email}
@returns(200) {data: [map]} # OK

--- END DOCUMENTATION ---

TASKS:
1. Create charge for $2000 USD with source tok_visa
2. Get charge details for ch_abc123
3. List all charges with limit 10
4. Capture uncaptured charge ch_xyz789 for full amount
5. Create customer with email customer@example.com and name 'Jane Doe'
6. Get customer details for cus_abc123
7. List customers with limit 20 and created after timestamp
8. Create charge for customer cus_abc123 with amount 5000 and description 'Service fee'
9. Capture charge ch_test123 for partial amount 1000 with receipt email
10. List charges for customer cus_xyz789 with starting_after cursor
