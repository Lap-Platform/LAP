TASK 1: const ws = new WebSocket('wss://wss-primary.slack.com/websocket'); ws.on('message', (data) => { const event = JSON.parse(data); if (event.type === 'message') { handleMessage(event); } });
TASK 2: ws.on('message', (data) => { const event = JSON.parse(data); if (event.type === 'reaction_added') { handleReaction(event); } });
TASK 3: ws.on('message', (data) => { const event = JSON.parse(data); if (event.type === 'presence_change') { handlePresenceChange(event); } });
TASK 4: ws.on('message', (data) => { const event = JSON.parse(data); if (event.type === 'channel_created') { handleChannelCreated(event); } });
TASK 5: if (event.type === 'message') { const { channel, user, text, ts } = event; processMessage(channel, user, text, ts); }
TASK 6: if (event.type === 'reaction_added') { const { user, reaction, item } = event; const { channel, ts } = item; logReaction(user, reaction, channel, ts); }
TASK 7: if (event.type === 'presence_change') { const { user, presence } = event; updateUserPresence(user, presence); }
TASK 8: if (event.type === 'channel_created') { const { id, name } = event.channel; createChannelRecord(id, name); }
TASK 9: app.post('/slack/events', (req, res) => { const { type, challenge } = req.body; if (type === 'url_verification') { res.json({ challenge }); } });
TASK 10: if (event.type === 'message' && event.channel === 'C123ABC') { const messageText = event.text; processChannelMessage(messageText); }