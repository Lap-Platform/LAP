TASK 1: # WebSocket connection: ws = new WebSocket('wss://wss-primary.slack.com/websocket?token=YOUR_TOKEN'); ws.on('message', event => { if (event.type === 'message') handleMessage(event); });
TASK 2: # WebSocket event handler: ws.on('message', event => { if (event.type === 'reaction_added') handleReaction(event); });
TASK 3: # WebSocket event handler: ws.on('message', event => { if (event.type === 'presence_change') handlePresence(event); });
TASK 4: # WebSocket event handler: ws.on('message', event => { if (event.type === 'channel_created') handleChannel(event); });
TASK 5: # Handle message: const { channel, user, text, ts } = event;
TASK 6: # Handle reaction: const { reaction, user, item } = event; const { channel, ts } = item;
TASK 7: # Handle presence: const { user, presence } = event;
TASK 8: # Handle channel: const { id, name } = event.channel;
TASK 9: # URL verification: if (event.type === 'url_verification') { return { challenge: event.challenge }; }
TASK 10: # Filter messages: ws.on('message', event => { if (event.type === 'message' && event.channel === 'C123ABC') { const text = event.text; processMessage(text); } });
